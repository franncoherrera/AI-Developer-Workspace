# Vue Component Prompt

Create a Vue 3 component with:

1. `<script setup lang="ts">` — Composition API + TypeScript
2. `<template>` — con Fundamental Library / UI5 components
3. Pinia store para estado (si aplica)
4. Llamadas a CAP via `services/` (axios con interceptors SAP)
5. Loading, empty, error states explícitos
6. Tests en `tests/` con Vitest + Vue Test Utils
